

//Part 2: Predict the Output Snippet A

let score = 72;
let result = score >=50 ? "pass" : "fail"
console.log(result);  // will output  pass

// Snippet B

let cartTotal = 0;
let message = cartTotal > 0 ? "Proceed to checkout" : "Your cart is empty"
console.log(message);  // will output   Your cart is empty

// Snippet C

let stock = 5;
console.log(`Stock status: ${stock > 0 ? "Available" : "Out of stock" }`);  // Stock status Available